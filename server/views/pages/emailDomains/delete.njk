{% extends "../../partials/layout.njk" %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/breadcrumbs/macro.njk" import govukBreadcrumbs %}
{% from "govuk/components/error-summary/macro.njk" import govukErrorSummary %}
{% from "govuk/components/warning-text/macro.njk" import govukWarningText %}
{% from "govuk/components/input/macro.njk" import govukInput %}

{% set title = "Delete Email Domain" %}

{% block beforeContent %}
  {{ govukBreadcrumbs({
    items: [
      { text: "Home", href: homeUrl },
      { text: "Manage user accounts", href: '/' },
      { text: "Manage Email Domains", href: listUrl },
      { text: title }
    ]
  }) }}
{% endblock %}
{% block content %}
  {% if errors.length > 0 %}
    {{ govukErrorSummary({
      titleText: "There is a problem",
      errorList: errors,
      attributes: { 'data-qa': 'error-summary'}
    }) }}
  {% endif %}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <span class="govuk-caption-xl">{{ domain }}</span>
      <h1 class="govuk-heading-l">{{ title }}</h1>
      <form action="{{ deleteUrl }}" method="POST">
        <input type="hidden" name="_csrf" value="{{ csrfToken }}"/>

        {{ govukWarningText({
          text: "This action cannot be undone",
          iconFallbackText: "Warning"
        }) }}

        {% set hintHtml %}
          Enter <span class="govuk-!-font-weight-bold" data-qa="domain-name">"{{ domain }}"</span> to confirm
        {% endset %}

        {{ govukInput({label: {
          text: "Are you sure you want to delete this domain?", classes: "govuk-label--s"
        },
          hint: {
            html: hintHtml
          },
          classes: "govuk-input--width-30",
          value: confirmedDomain,
          id: "confirmedDomain",
          name: "confirmedDomain",
          errorMessage: errors | findError('confirmedDomain')
        }) }}

        <div class="govuk-button-group">
          {{ govukButton({
            text: "Delete Email Domain",
            preventDoubleClick: true,
            type: "submit",
            classes: "govuk-button--warning",
            attributes: { "data-qa": "delete-domain-button" }
          }) }}

          {{ govukButton({
            text: "Cancel",
            element: 'a',
            href: listUrl,
            classes: "govuk-button--secondary",
            attributes: { "id": "cancel-button" }
          }) }}
        </div>
      </form>
    </div>
  </div>
{% endblock %}
